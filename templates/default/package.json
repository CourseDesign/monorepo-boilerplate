{
  "name": "template-default",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc",
    "test": "",
    "lint": "",
    "prepublishOnly": "npm run build"
  }
}
